version: '3.7'
services:
  web: 
    image: nginx
    init: true
    container_name: frontend-nginx
    volumes:
        - ../Front/.:/usr/share/nginx/html
        - ./default.conf:/etc/nginx/conf.d/default.conf 
    ports:
      - "80:80"
